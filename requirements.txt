# --- Shared ---
python-dotenv==1.0.1
pydantic

# --- Backend (Dev B: app/) ---
# fastapi unpinned: 0.115.0 breaks at import time ("Router.__init__() got an
# unexpected keyword argument 'on_startup'") against modern starlette (>=1.0)
# which pip resolves to when no upper bound is set - install latest of both together.
fastapi
uvicorn[standard]==0.30.6
requests==2.32.3
boto3==1.35.24
faiss-cpu==1.12.0
numpy==2.3.3

# --- UI (Dev B: ui/) ---
streamlit

# --- Ingestion (Dev A: ingest/) ---
beautifulsoup4==4.12.3
lxml
pillow
# --- Tests (shared) ---
pytest