# Secrets / environment
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Session data / knowledge bases (keep the fixture, ignore real crawls)
data/sessions/*
!data/sessions/fixture_demo/
!data/sessions/.gitkeep

# SQLite databases (real app data + test artifacts must never be committed)
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
data/test_sessions/

# OS / editor
.DS_Store
.vscode/